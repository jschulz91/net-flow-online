export type DiagramMode = 'IST' | 'SOLL';
export type ViewType = 'swimlane' | 'sequence';

export type DiagramMeta = {
  title: string;
  creator: string;
  version: string;
  createdAt: string;
  updatedAt: string;
  description: string;
};
